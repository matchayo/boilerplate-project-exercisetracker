const express = require('express');
const router = express.Router();
const Exercise = require('../schemas/exercises');

router.post('/', function(req, res) {
    
});

router.get('/', function(req, res) {
});

module.exports = router;
